# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Environment variables / secrets
*.env
.env.*
secrets.toml
config.local.*

# Jupyter Notebook
.ipynb_checkpoints/
*/.ipynb_checkpoints/*
profile_default/
ipython_config.py

# IPython
.python-version

# pyenv / pipenv / poetry
Pipfile.lock
poetry.lock

# Data files (often large or sensitive — track via DVC/remote instead)
data/*.csv
data/*.xlsx
data/*.xls
data/*.parquet
data/*.json
data/*.db
data/*.sqlite
!data/.gitkeep

# --- Committed datasets (intentionally version-controlled) ---
# The coffee-sales dataset is public-domain and small, so we track it in-repo.
# (The data/*.csv rules above only ignore loose files dropped directly in data/.)
!data/coffee-sales/

# Models / large binaries
*.pkl
*.joblib
*.h5
*.pt
*.pth

# Testing / coverage
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.cache
nosetests.xml
coverage.xml
*.cover

# Type checkers / linters
.mypy_cache/
.dmypy.json
.ruff_cache/

# Claude Code personal settings (local, not shared)
.claude/settings.local.json

# IDEs and editors
.idea/
.vscode/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db
desktop.ini
